// const performanceItems = document.querySelectorAll('.performance_list__item');
// const performanceContainer = document.querySelector('.performance_container');

// performanceItems.forEach((i) => {
//   i.addEventListener('mouseenter', (e) => {
//     const { position } = e.target.dataset;
//     performanceContainer.classList.add(`${position}`);
//   });
//   // i.addEventListener('mouseleave', (e) => {
//   //   const { position } = e.target.dataset;
//   //   performanceContainer.classList.remove(`${position}`);
//   // });

//   i.addEventListener('click', (e) => {
//     const { position } = e.target.dataset;
//     // const bound = e.target.getBoundingClientRect();
//     // console.log('🚀 ~ file: performance.js:7 ~ i.addEventListener ~ bound', bound);
//     // item.classList.toggle('active');
//     // item.style = '--w: 1000px; --h: 500px';
//     performanceContainer.classList.add(`active`);
//   });
// });

// performanceContainer.addEventListener('click', () => {
//   performanceContainer.classList.remove('active', 'right');
// });
